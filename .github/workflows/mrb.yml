name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions: write-all

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: copy dashboard
      run: cp "./dash_app.py" "./online/dash_app.py"
    - name: copy utils
      run: cp -a "./utils/." "./online/"
    - name: copy ytdb
      run: cp -a "./ytdb/." "./online/"
