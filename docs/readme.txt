Documentation is built using the given Makefile.

To build on Unix:
    $ make <builder>

To build on Windows:
    $ .\make.bat <builder>

"builder" is one of Sphinx's supported builders, eg. "html", "latex", etc.
